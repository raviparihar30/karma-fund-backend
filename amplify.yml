version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    baseDirectory: ""
    files: []
backend:
  phases:
    # Build and deploy Backend Environment
    build:
      commands:
        - npm install
    # Configure the backend environment
    configure:
      commands:
        - amplify push --yes
  appRoot: .
