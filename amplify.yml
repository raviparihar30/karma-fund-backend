version: 1
backend:
  phases:
    # Build and deploy Backend Environment
    build:
      commands:
        - npm install
    # Configure the backend environment
    configure:
      commands:
        - amplify push --yes
  appRoot: .
