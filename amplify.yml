version: 1
backend:
  phases:
    # Build and deploy Backend Environment
    build:
      commands:
        - npm install
    # Configure the backend environment
    configure:
      commands:
        - amplify push --yes
  artifacts:
    # Set a valid base directory, even if there's no frontend
    baseDirectory: ""
    files: []
  appRoot: .
